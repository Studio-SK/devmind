spring.datasource.url=jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:template_test}

spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true

app.jwt.secret=${JWT_SECRET:test-secret-at-least-256-bits-long-for-hmac-signing}
